import { SafeAreaView, StyleSheet, Text, View } from "react-native";
import React from "react";

const FAQPage = () => {
  return (
    <SafeAreaView className="flex-1 bg-pink-200 px-5 py-5">
      <View className="flex-row my-4 p-3 bg-white rounded-md">
        <Text>Are the courses free?</Text>
      </View>
    </SafeAreaView>
  );
};

export default FAQPage;

const styles = StyleSheet.create({});
